# MediaEngine
Media Engine is a multimedia engine, the `Features` section show its abilities.

See `Dockerfile` to build MediaEngine project.

### Features

- [ ] Decode and resample from audio file or memory.